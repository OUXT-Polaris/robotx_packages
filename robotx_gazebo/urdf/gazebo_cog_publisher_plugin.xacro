<?xml version="1.0"?>
<robot xmlns:xacro="http://ros.org/wiki/xacro">
  <xacro:macro name="gazebo_cog_publisher_plugin.xacro" params="name">
    <gazebo>
      <plugin name="gazebo_cog_publisher_${name}" filename="libgazebo_cog_publisher_plugin.so">
    </gazebo>
  </xacro:macro>
</robot>